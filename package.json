{
  "name": "khushali-portfolio",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "npm run generate:assets && vite --host 127.0.0.1",
    "build": "npm run generate:assets && vite build",
    "preview": "vite preview --host 127.0.0.1",
    "generate:assets": "node scripts/generateAssetManifest.mjs",
    "generate:previews": "npm run generate:assets && node scripts/generateDeployAssets.mjs && npm run generate:assets"
  },
  "dependencies": {
    "@vitejs/plugin-react": "^4.3.4",
    "framer-motion": "^12.40.0",
    "lucide-react": "^0.468.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0",
    "vite": "^6.0.5"
  }
}
