# portfoliowebsite

Advanced Vite React portfolio website for Khushali Bochiwal.

## Scripts

- `npm run dev` starts the local Vite server.
- `npm run build` creates the production build.
- `npm run preview` serves the production build locally.
